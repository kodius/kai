# TypeScript Types

Always read `.kai/types-common.md`.

When using utility types (Omit, Pick, Partial, Record), read `.kai/types-utility-types.md`.

When working with Zod schemas or runtime validation, read `.kai/types-zod.md`.
