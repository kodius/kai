# React

Always read `.kai/react-common.md`.

When doing conditional rendering, read `.kai/react-conditional-rendering.md`.

When using React Query for data fetching or mutations, read `.kai/react-query.md`.
