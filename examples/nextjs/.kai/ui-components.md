# UI Components

Component-specific usage rules and gotchas for shared UI components.

## Spinner — self-centering, don't wrap in a layout div

`Spinner` renders its own centering wrapper. Pass `className` for sizing — do not wrap it in a `flex items-center justify-center` div.
